/** @type {import('prettier').Config} */
const config = {
  bracketSpacing: true,
  jsxSingleQuote: true,
  plugins: ['prettier-plugin-tailwindcss'],
  printWidth: 100,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'all',
};

export default config;
